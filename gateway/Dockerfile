# Создать образ на основе базового слоя
# В качестве базового слоя взять с Docker Hub готовый образ nginx:1.22.1
FROM nginx:1.22.1

# Скопировать в создаваемый образ конфиг-файл nginx.conf (при выполнении инструкции из докерфайла мы "стартуем" с папки где лежит докерфайл, поэтому никуда переходить не нужно),
# просто указывваем копируемый файл, т.к. он лежит вмете в докерфайлом
# в образе разместим его в /etc/nginx/templates/ и переименуем конфиг-файл в default.conf.template
COPY nginx.conf /etc/nginx/templates/default.conf.template 